{
  "name": "graph-explorer-json-query",
  "version": "0.4.0",
  "private": true,
  "description": "Browser extension (Chrome/Edge) adding JMESPath JSON query capabilities to Microsoft Graph Explorer results.",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "icons": "node scripts/make-icons.js",
    "package": "mkdir -p dist && zip -r dist/graph-explorer-json-query.zip manifest.json icons popup src vendor -x '*.DS_Store'"
  }
}
